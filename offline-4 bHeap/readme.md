how to run::
g++ -std=c++17 partA.cpp -o partA
./partA test_01/input.txt myout1.txt

if run partB:
to invoke the visualize heap: add command V h
means visualize visualize heap h.. example V 1
to invoke visualize the union of heaph h1 and h2

use VU h1 h2